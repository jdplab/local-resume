[uwsgi]
module = app:app  # Replace 'app' with the name of your Flask application variable
socket = /srv/frontend/flask_app.sock  # Specify the UNIX socket file path
chmod-socket = 660  # Set permissions for the socket file
vacuum = true  # Clean up socket file when uWSGI stops
die-on-term = true  # Die on receiving SIGTERM signal
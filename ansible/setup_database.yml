- name: Mount DB Share
  hosts: dbservers
  remote_user: root
  become: yes
  become_method: sudo

  tasks:
    - name: Mount NFS Share
      mount:
        name: /mnt/nfs
        src: "192.168.69.200:/volume1/resume-db"
        fstype: nfs
        opts: defaults
        state: mounted


